// HJ15 求int型正整数在内存中存储时1的个数
#include <algorithm>
#include <iostream>

#if 1
auto main(int argc, char* argv[]) -> int
{
    int n {};
    std::string str {};
    while (std::cin >> n) {
    }
    return 0;
}
#endif

#if 0
#include <iomanip>
本打算使用c++流算子，直接进行进制转换，但是，流算子只支持8、10、16进制
#endif